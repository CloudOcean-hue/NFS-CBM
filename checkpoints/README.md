# Local checkpoint directory

Place the exported inference checkpoint here as `nfs_cbm_inference.pt`, or change `checkpoint_path` in the local configuration.

Checkpoint files are excluded from Git and are not distributed with this repository.
